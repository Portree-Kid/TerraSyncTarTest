pipeline {
   agent any
   options { disableConcurrentBuilds() }
   stages {

    stage( 'build' ) {
      steps{
            dockerNode(credentialsId: '', dockerHost: 'localhost', image: 'TerraSyncTarTest', remoteFs: '') {
                // some block
            }
       }
    }
  }
}
